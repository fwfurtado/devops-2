language: java

services:
  - docker

jobs:
  include:
    - stage: test
      name: 'Testing application'
      services:
        - postgresql
      script:
        - sh gradlew test
    - stage: package
      name: 'Packaging application'
      script:
        - sh gradlew build -x test